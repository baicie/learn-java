AegisOps has been installed.

Release: {{ .Release.Name }}
Namespace: {{ .Release.Namespace }}

Enabled business processes:
{{- range $appName, $app := .Values.apps }}
{{- if $app.enabled }}
  - {{ include "aegisops.fullname" $ }}-{{ $appName }}:{{ $app.port }}
{{- end }}
{{- end }}

Security:
  - App and Agent use workload mTLS plus short-lived Ed25519 Diagnosis Grants.
  - Runner uses a separate restricted PostgreSQL identity and verifies Execution Grants.
  - Existing component Secrets must provide the keys documented in values.yaml.
  - Provision Runner table grants with deploy/init/002-grant-runner.sql before enabling it.

Next:
  kubectl get pods -n {{ .Release.Namespace }}
  kubectl get svc -n {{ .Release.Namespace }}
